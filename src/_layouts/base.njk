<!DOCTYPE html>
<html lang="{{ locale }}" dir="{{ dir }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ 'title' | i18n }}</title>
  <base href="/">
  <link rel="stylesheet" href="{{ paths.styles }}/styles.css"/>
</head>
<body>
  {# content #}
  {% block baseContent %}
    {{ content | safe }}
  {% endblock baseContent %}
  {# content #}
</body>
</html>